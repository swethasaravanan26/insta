import React from 'react';
import ReactDOM from 'react-dom';
// Ensure index.css exists or remove this line if unnecessary
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
